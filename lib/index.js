"use strict";

/**
 * everyReminder
 * Displays a reminder notification every interval.
 *
 * @name everyReminder
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function everyReminder (a, b) {
    return a + b;
};